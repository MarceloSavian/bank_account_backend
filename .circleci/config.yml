version: 2.1
jobs:
  lintandtest:
    docker:
      - image: circleci/node:12.4
    working_directory: ~/repo
    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run test:coveralls
workflows:
  version: 2
  build:
    jobs:
      - lint